/* eslint-disable */
export enum EnumWidget {
  YOUR_PORTFOLIO = "your portfolio",
  DIGITAL_ASSETS = "digital assets",
  FIAT_CURRENCIES = "fiat currencies",
  FEATURE_HIGHLIGHT = "feature highlight",
}

export enum State {
  ENABLED = "enabled",
  DISABLED = "disabled",
}

export enum PortfolioColumns {
  BROKERAGE = "brokerage",
  EXCHANGE = "exchange",
  LEVERAGE = "leverage",
  BUY = "unsettled-buy",
  SELL = "unsettled-sell",
}
